<%= form_with(model: park, local: true) do |form| %>
  <% if park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
      <% park.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :park_name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.number_field :location, id: :park_location %>
  </div>

  <div class="field">
    <%= form.label :neighborhood %>
    <%= form.text_field :neighborhood, id: :park_neighborhood %>
  </div>

  <div class="field">
    <%= form.label :park_url %>
    <%= form.text_field :park_url, id: :park_park_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
